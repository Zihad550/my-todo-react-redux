import styled from 'styled-components';

const Icon = styled.img`
    width: 1.5rem;
    cursor: pointer;
    display:inline-block;
    &:first-child{
        margin-right:.5rem;
    }
`;

export default Icon;
